# Designer Studio QA — v3.35.1

Validation targets:

- 8 Designer modules remain available.
- 7-stage Designer learning loop includes **HEAR THE DESIGNERS**.
- 3 industry deep dives per module = 24 total.
- exactly 1 click-to-load video per module = 8 embedded videos.
- every deep dive has a HTTPS original source, focus prompt, watch/read prompt and transfer task.
- every video has a clean YouTube ID and uses `youtube-nocookie.com` only after user interaction.
- all existing Designer recipe IDs and Studio Build completion IDs remain unchanged.
- browser Back scroll restoration is implemented independently of module completion state.
- all JavaScript passes syntax validation.
