# v3.44.1 — Blueprint Foundations Classroom Visual Pass

This supersedes v3.44.0. The Blueprint Foundations file is not yet on GitHub main, so you do not need to upload v3.44.0 first.

## Upload
- `index.html`
- `data/blueprint-foundations-learning.js`
- complete folder `assets/tutorials/blueprint-foundations/`

## What changed
13 real classroom Unreal screenshots have been optimised and placed at the exact point students need them.

### Variable Types
- real variable type picker
- Object Reference variable Details
- Get vs Set Boolean nodes

### Data → Decisions
- Keys Owned >= Keys Required → Branch
- caption explicitly identifies DATA → TEST → DECISION

### Object References
- Instance Editable reference setup
- placed Blueprint Details showing where the reference is assigned
- `None` is explicitly explained as “not assigned yet”

### Functions
- real built-in Apply Damage node used honestly as a visual example of a function-style call
- guide explicitly says it is not the custom ApplyDamage function students build

### Arrays
- Single → Array container menu
- Vector Array example used only to teach the container UI
- For Each Loop with Loop Body / Array Element / Index / Completed explained

### Enums
- create/search for Enumeration
- Enum asset example
- choosing the custom Enum as a variable type
- Switch on Enum

## Building Blocks
The same authentic screenshots are also attached to the relevant existing Building Blocks:
- Variables, Types, Get & Set
- Branches, Switches & Select
- Object Reference vs Class Reference
- Arrays, Sets & Maps
- Enums

This means the images help whether students arrive through the new Level 4 pathway or by browsing Unreal Building Blocks.

## Still optional later
No blocker, but if you want a completely illustrated pass later, the two useful remaining captures are:
- the actual custom `ApplyDamage` Function open with its `DamageAmount` input
- Struct / Map setup screenshots

## Safety
No changes to:
- app.js
- backend.js
- original course-data.js
- original building-block-data.js
- original tutorial-data.js
- original pathway-data.js
- Prison Cell patch
- Supabase
- roster controls
- visual CSS
