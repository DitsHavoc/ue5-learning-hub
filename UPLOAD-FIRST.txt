UE5 Learning Hub v3.56.0 — LOCAL ONLY / NO ACCOUNTS
====================================================

UPLOAD THESE THREE FILES TO THE SAME PATHS IN THE REPOSITORY:

1. data/supabase-config.js
2. backend.js
3. signup-feedback.js

Why this is the safe emergency change:
- Supabase configuration contains no URL or key.
- backend.js is now a local-only compatibility layer and cannot create a cloud client.
- signup-feedback.js is repurposed as the privacy lockdown layer so the existing index.html already loads it.
- Account, signup, class, teacher, leaderboard, cloud request and account-backed news controls are removed/blocked.
- Critique Board becomes a static Critique Guide: no posts or uploads.
- My Progress remains, but uses browser localStorage only.
- Existing lesson/project practice progress stays on that browser.
- Formal submissions and feedback stay in Microsoft Teams.

IMPORTANT
The old Supabase project should be deleted after confirming this patch is live. Deleting the whole project is cleaner than trying to delete database rows and Storage objects separately.

After upload, hard refresh once (Ctrl+F5) and check:
- no Sign in / Create account button
- no Classes / Teacher / Leaderboard / Feature Requests
- Critique says Critique Guide
- version badge says v3.56.0 • LOCAL ONLY
- My Progress still works
- lesson completion survives a page refresh on the same browser
